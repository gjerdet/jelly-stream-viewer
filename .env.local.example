# ============================================
# LOKAL DEPLOYMENT KONFIGURASJON
# ============================================
# Kopier denne filen til .env.local og tilpass verdiene
# For lokal PostgreSQL deployment (Docker Compose)

# ============================================
# DATABASE KONFIGURASJON
# ============================================

# PostgreSQL database navn
DB_NAME=jellystream

# PostgreSQL brukernavn
DB_USER=jellystream

# PostgreSQL passord (ENDRE DETTE!)
DB_PASSWORD=changeme_secure_password_here

# PostgreSQL port (standard: 5432)
DB_PORT=5432

# ============================================
# PGADMIN (VALGFRITT - for database admin)
# ============================================

# pgAdmin epost (for innlogging)
PGADMIN_EMAIL=admin@jellystream.local

# pgAdmin passord
PGADMIN_PASSWORD=admin

# pgAdmin port
PGADMIN_PORT=5050

# ============================================
# APPLICATION KONFIGURASJON
# ============================================

# For lokal deployment uten Supabase Cloud:
# Connection string til lokal PostgreSQL
DATABASE_URL=postgresql://${DB_USER}:${DB_PASSWORD}@localhost:${DB_PORT}/${DB_NAME}

# Dummy values for lokal auth (hvis du ikke bruker Supabase Cloud)
# Disse brukes bare for kompatibilitet med koden
VITE_SUPABASE_URL=http://localhost:54321
VITE_SUPABASE_PUBLISHABLE_KEY=local-dev-key
VITE_SUPABASE_PROJECT_ID=local

# ============================================
# NOTATER
# ============================================
# 
# Lokal deployment (uten Supabase Cloud):
# - Auth vil ikke fungere uten Supabase CLI eller egen auth-løsning
# - Edge functions vil ikke fungere
# - Du kan fortsatt bruke Jellyfin direkte
# 
# Anbefalt: Bruk Supabase Cloud for auth + lokal PostgreSQL for data
# Se DEPLOYMENT_LOCAL.md for mer info
